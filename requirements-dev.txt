# Development only -- the application itself needs psutil + PyYAML
# (and optionally psycopg2 for the PostgreSQL panel).
#
#   python3 -m venv .venv
#   .venv/bin/pip install -r requirements-dev.txt -e .
#
psutil>=5.6
PyYAML>=5.1
psycopg2-binary>=2.8
pytest>=7.0
ruff>=0.1
build>=1.0
